<?php

class Item extends Model {

    // Inherits selectAll(), select($id), query(), etc. from Model → SQLQuery
    // Table: `items` (auto-resolved by Model base class)

    // Add custom Item-specific queries here in Part 2+
}
